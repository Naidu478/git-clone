# Git Workflow Assignment

## Steps Completed
1. Cloned repository using `git clone`.
2. Created new branch `feature-update` and switched using `git checkout`.
3. Edited `task.html` by adding a new paragraph.
4. Staged changes with `git add` and committed using `git commit -m "Added new paragraph to task.html"`.
5. Merged branch back into `main` using `git merge`.
6. Verified commit history with `git log`.
7. Pushed updated repository to remote using `git push`.

## Proof
Screenshots of commands are included in the repository:
- Screenshot 2026-06-22 184718.png
- Screenshot 2026-06-22 184735.png

## Repository Link
[GitHub Repository](https://github.com/Naidu478/git-clone)

## Updated HTML File
```html
<p>This is my updated paragraph for the assignment.</p>
